"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const devkit_1 = require("@nx/devkit");
/*
 *
 */
async function update(tree) {
    for (const [name, config] of (0, devkit_1.getProjects)(tree)) {
        if (config.targets?.build?.executor !== '@nrwl/web:rollup')
            continue;
        if (Array.isArray(config.targets.build.options?.format))
            continue;
        config.targets.build.options = {
            ...config.targets.build.options,
            format: ['esm', 'cjs'],
        };
        (0, devkit_1.updateProjectConfiguration)(tree, name, config);
    }
    await (0, devkit_1.formatFiles)(tree);
}
exports.default = update;
